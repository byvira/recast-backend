Translate the following template into the language identified by the code or name '{{ language }}'. This is a template with placeholder tokens like {example} — you MUST preserve every placeholder token EXACTLY as written, character-for-character, including the curly braces and the name inside them. Do not translate, rename, or alter the placeholder names. Translate only the surrounding natural-language text around them.

TEMPLATE:
{{ english_template }}

Return ONLY the translated template text. No explanation, no quotes, no markdown.